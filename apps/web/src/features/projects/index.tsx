import { PagePlaceholder } from "../../components/shared/page-placeholder";

export function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Search, filter, and manage projects within your access scope."
    />
  );
}

export function ProjectDetailPage() {
  return (
    <PagePlaceholder
      title="Project detail"
      description="Project summary, members, milestones, documents, and task views."
    />
  );
}
