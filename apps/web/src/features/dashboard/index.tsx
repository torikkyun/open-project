import { PagePlaceholder } from "../../components/shared/page-placeholder";

export function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="Track project progress, workload, overdue work, and review queue."
    />
  );
}
