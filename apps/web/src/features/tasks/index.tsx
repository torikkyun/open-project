import { PagePlaceholder } from "../../components/shared/page-placeholder";

export function TaskDetailPage() {
  return (
    <PagePlaceholder
      title="Task detail"
      description="Task status, schedule, hierarchy, review, history, and collaboration."
    />
  );
}
